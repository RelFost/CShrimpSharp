# Релизы и публикация пакетов

Автоматический workflow собирает проект, запускает тесты, создаёт `.nupkg`, публикует GitHub Packages, по выбору публикует nuget.org, формирует архив исходников и создаёт GitHub Release. Подробные команды приведены в ответе вместе с архивом.
