# Транзакции

`ShrimpTransaction` — стек явных компенсаций, а не транзакция базы данных. Компенсацию следует регистрировать сразу после обратимого изменения либо использовать `StepAsync`. Откат выполняется в обратном порядке.
