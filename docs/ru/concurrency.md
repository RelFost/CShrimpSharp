# Конкурентность

`SyncAsync` совместно запускает управляемые операции и ожидает все результаты. Типизированные перегрузки возвращают кортежи. `RaceAsync` выбирает первое завершение, отменяет проигравших и наблюдает их. `WithTimeoutAsync` использует кооперативную отмену.
