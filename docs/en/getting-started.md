# Getting started

Install with `dotnet add package CShrimpSharp --prerelease`, then import `CShrimpSharp`, `CShrimpSharp.Collections`, `CShrimpSharp.Concurrency`, or `CShrimpSharp.Transactions` as needed.
