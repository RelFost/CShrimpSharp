# Release and package publication

See the root README and the Russian mirror for exact commands. The automated workflow builds, tests, packs, publishes to GitHub Packages, optionally publishes to nuget.org, creates a source archive, and creates a GitHub Release.
