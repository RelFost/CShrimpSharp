# Architecture

The preview remains one runtime package. Namespaces separate Core, Collections, Concurrency, and Transactions so packages can be split later only when independent dependencies or versioning justify it.
