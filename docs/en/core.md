# Core types

`Option<T>` represents presence or absence. `Result<TValue,TError>` represents success or expected failure. `Validation<TValue,TError>` accumulates multiple validation errors instead of stopping at the first one.
